#include <stdio.h>

int main() {
    //AISHI DE
    printf("Greatest of two numbers\n\n");

    int a = 50;
    int b = 460;

    if (a > b) {
        printf("the value of a that is %d is greater than value of b which is %d", a, b);
    }
    else {
        printf("the value of b that is %d is greater than value of a which is %d", b, a);
    }

    return 0;
}